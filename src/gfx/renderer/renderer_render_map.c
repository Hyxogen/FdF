#include "../renderer.h"

t_matrix4f
	_get_center_trans(const t_map *map)
{
	t_fl32	x_min;

void
	render_map(t_image_buffer *buffer,
			const t_matrix4f *transformation, const t_map *map, t_color color)
{
	
	(void)transformation;
}

